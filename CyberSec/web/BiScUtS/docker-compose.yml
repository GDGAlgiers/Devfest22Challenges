version: "3.8"
services:
  server:
    build: .
    ports:
      - '1337:5000'
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
      resources:
        limits:
          memory: 50M
